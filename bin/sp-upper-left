#!/bin/bash
if [ "$HOST" = "rackspace" ] ; then
    osascript -e "tell application \"$1\" to set bounds of front window to {0, 23, 1030, 750}"
else
    osascript -e "tell application \"$1\" to set bounds of front window to {0, 22, 1030, 750}"
fi
