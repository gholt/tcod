#!/bin/bash
if [ "$HOST" = "ML304ZFFT3" ] ; then
    osascript -e 'tell application "Terminal" to set bounds of front window to {1030, 23, 1680, 1050}'
else
    osascript -e 'tell application "Terminal" to set bounds of front window to {1030, 22, 1680, 1050}'
fi
